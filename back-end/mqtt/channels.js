const { measurements } = require("../constants");

const ch_temperature = 'measurement/temperature';
const ch_sound = 'measurement/sound';
const ch_heart = 'measurement/heart';
const ch_sleep = 'measurement/sleep';
const ch_walking = 'measurement/walking';
const ch_jogging = 'measurement/jogging';
const ch_steps = 'measurement/steps';
const ch_biking = 'measurement/biking';
const ch_idle = 'measurement/idling';

module.exports = {
    ch_temperature,
    ch_sound,
    ch_heart,
    ch_sleep,
    ch_walking,
    ch_jogging,
    ch_steps,
    ch_biking,
    ch_idle
};
